# Session secret for secure session management
SESSION_SECRET=change-me-to-a-random-string

# Mailgun API configuration for sending emails
# Get these from your Mailgun account: https://app.mailgun.com/
MAILGUN_API_KEY=your-mailgun-api-key-here
MAILGUN_DOMAIN=your-mailgun-domain-here

# Admin email for monitoring alerts and system notifications
ADMIN_EMAIL=avarsanyi2@nebraska.edu

# Optional: Override default data directory (useful to avoid iCloud sync)
# MYOT_DATA_DIR=/path/to/data/directory

# Optional: Set to production for production environment
# NODE_ENV=production
